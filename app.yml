{
  "$schema": "./pennsieve-app-schema.json",
  "schemaVersion": "1.0.0",
  "application": {
    "id": "pytorch-example",
    "name": "PyTorch GPU Example",
    "description": "Demo application that detects CUDA-capable GPUs and reports the device name and CUDA version using PyTorch.",
    "version": "1.0.1",
    "type": "processor",
    "maintainers": [
      {
        "name": "edmore"
      }
    ],
    "tags": [
      "pytorch",
      "gpu",
      "demo"
    ]
  },
  "runtime": {
    "cpu": 1024,
    "memory": 2048,
    "computeTypes": [
      "gpu"
    ],
    "timeoutSeconds": 300
  },
  "parameters": [],
  "commandArguments": [],
  "inputs": [
    {
      "name": "package_file",
      "description": "Pipeline package file.",
      "mediaTypes": [
        "application/octet-stream"
      ],
      "cardinality": "one",
      "required": true
    }
  ],
  "outputs": [
    {
      "name": "package_file",
      "description": "Pipeline package file.",
      "mediaTypes": [
        "application/octet-stream"
      ]
    }
  ]
}
